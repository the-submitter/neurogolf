I'm attempting the Kaggle NeuroGolf Championship 2026:
web references: `https://www.kaggle.com/competitions/neurogolf-2026/overview` and `https://www.kaggle.com/competitions/neurogolf-2026/data`.

Use the current repo which contains `kaggle_tasks_data/task<task-num>.json` (task-num: 001 to 400) and corresponding ARC-GEN generator `generate()` and validator `validate()` in `ARC-GEN/tasks/task_<task-id>.py`. `ARC-GEN` is the entire ARC-GEN git repo with recursive submodules contained as well. `utils/task_principles.json` contains the mapping from <task-num> to <task-id> along with corresponding `principle` used to generate that tasks data (Eg. "fractal", "honeypots", "clones", etc.). `utils/task_map.json` just has the mapping between <task-num> and <task-id> without `principle`. `utils/neurogolf_utils.py` has the official scorer metric along with additional utility/helper functions. `utils/the-2026-neurogolf-championship.py` is just a sample notebook py:percent script showcasing the use of the official scorer metric and other optional utilities from `utils/neurogolf_utils.py`.

The current kaggle leaderboard score is 8242.59 (~20.6065 per task on average) which means `80.93` is the current approximate average cost (memory + parameters) per ONNX task graph. So build ONNX graphs with this optimal budget in mind. Optimize for the official score metric as attached in `utils/neurogolf_utils.py`. As a suggestion, the `Einsum` op (opset 12) seems like a good ONNX candidate for cost-effective ways as it did work for task001, so could be worth a shot for other tasks as well. You can analyze the `generate()` generator (and related code) in `ARC-GEN/tasks/task_<task-id>.py` and attempt to reverse-engineer it to create/optimize the ONNX graph. Any opset can be used which is supported by the official scorer metric.

Based on all this, build the best ONNX graph which you can come up with to optimize for the official score metric.

Only attempt task {{TASK_NUMBER}} for now in your task's specific workspace `tasks/task{{TASK_NUMBER}}/`. You are free to use your task's specific workspace `tasks/task{{TASK_NUMBER}}/` however you want. Once done, also copy the final best optimized `.onnx` model into `submission/` as `submission/task{{TASK_NUMBER}}.onnx`. Use `~/.venv` to compile/test/run code and install libraries. Also maintain a task specific `tasks/task{{TASK_NUMBER}}/README.md` which has an explanation for the implementation/build of the final best optimized `.onnx` model along with cost (memory + parameters) and official metric score among other things. If this `tasks/task{{TASK_NUMBER}}/README.md` already exists, you may use it and/or other artifacts to build and improve upon an already existing optimal solution.
